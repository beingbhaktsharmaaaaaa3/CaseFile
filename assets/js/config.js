/* One-time setup: fill this in once after you create your GitHub repo.
   It powers the "Edit on GitHub" / "Create this entry" links throughout
   the site — clicking one takes you straight to GitHub's file editor
   for that exact topic, already scoped to the right path.

   Example:
   window.SITE_CONFIG = {
     githubRepoUrl: "https://github.com/hechan/casefile",
     branch: "main"
   };

   Leave githubRepoUrl empty ("") to hide these links until you set it.
*/
window.SITE_CONFIG = {
  githubRepoUrl: "",
  branch: "main"
};
